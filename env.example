# База данных MySQL
DB_DSN=app:yourpassword@tcp(127.0.0.1:3306)/tablegames?parseTime=true

# JWT секрет — минимум 32 символа, сгенерировать: openssl rand -hex 32
JWT_SECRET=replace_with_random_secret_min_32_chars

# Порт сервера
SERVER_PORT=8080
